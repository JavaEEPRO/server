const config = {
    connectionLimit : 10,
        host            : 'localhost',
        user            : 'root',
        password        : 'root',
        database        : 'books_koa2'
};

module.exports = config;